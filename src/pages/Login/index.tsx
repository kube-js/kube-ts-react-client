import React from 'react';
import { RouteProps } from 'react-router';
import LoginContainer from '../../containers/Login';

const Login = (props: RouteProps) => <LoginContainer {...props} />;

export default Login;
