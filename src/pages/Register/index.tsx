import React from 'react';
import { RouteProps } from 'react-router';
import RegisterContainer from '../../containers/Register';

const Register = (props: RouteProps) => <RegisterContainer {...props} />;

export default Register;
