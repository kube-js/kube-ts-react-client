import React from 'react';
import { RouterProps } from 'react-router';
import RegisterContainer from '../../containers/Register';

const Register = (props: RouterProps) => <RegisterContainer {...props} />;

export default Register;
